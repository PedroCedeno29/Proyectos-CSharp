string[]orders = ["B123", "C234","A345","C15","B177","G3003","C235","B179"];

foreach(string fraude in orders)
{
    if (fraude.StartsWith("B"))
    {
        Console.WriteLine($"The order: {fraude} attempts to be fraud!");
    }
}
